import classes from './Banner.module.css';
import bannerImg from '../../../assets/images/bg-header-desktop.svg';

const Banner = () => {
    return (
        <header className={classes.banner}>
            {/* <img className={classes['banner-image']} src={bannerImg} /> */}
        </header>
    )
}

export default Banner;